import { PartnerOrganization } from '../types';
export class PartnerOrganizationRepository { async save(item: PartnerOrganization): Promise<PartnerOrganization> { throw new Error('Not implemented'); } }
