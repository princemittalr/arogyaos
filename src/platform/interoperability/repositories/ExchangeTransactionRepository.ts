import { ExchangeTransaction } from '../types';
export class ExchangeTransactionRepository { async save(item: ExchangeTransaction): Promise<ExchangeTransaction> { throw new Error('Not implemented'); } }
