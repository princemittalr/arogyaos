import { TrustConfiguration } from '../types';
export class InteroperabilityConfigurationService {
  async manageGlobalInteropConfig(item: Partial<TrustConfiguration>): Promise<TrustConfiguration> { throw new Error('Not implemented'); }
}
