import { SearchAnalytics } from '../types';

export class SearchAnalyticsRepository {
  async save(analytics: SearchAnalytics): Promise<SearchAnalytics> {
    throw new Error('Not implemented');
  }
}
