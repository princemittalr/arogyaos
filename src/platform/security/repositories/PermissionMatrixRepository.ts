import { PermissionMatrix } from '../types';

export class PermissionMatrixRepository {
  async save(matrix: PermissionMatrix): Promise<PermissionMatrix> {
    throw new Error('Not implemented');
  }
}
